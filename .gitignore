# Virtual environment — never push this (it's huge and machine-specific)
venv/
.venv/
# Environment variables — contains your API key (SECRET)
.env

# Python cache files — auto-generated, not needed in repo
__pycache__/
*.pyc

# SQLite database — generated at runtime, not source code
*.db

# IDE settings 
.vscode/
.idea/

# OS files 
.DS_Store
Thumbs.db
